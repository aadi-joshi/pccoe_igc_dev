# Nugen Intelligence aligned-model inference.
#
# Everything works without these: the briefing falls back to a deterministic
# renderer and the schedule is identical either way. Set them to enable the
# aligned path.
#
# Obtain credentials only via the official pccoeigc.com prizes page.
# Verify the endpoint path and model id against docs.nugen.in for your account.

NUGEN_API_KEY=
# NUGEN_BASE_URL=https://api.nugen.in/inference
# NUGEN_MODEL=
